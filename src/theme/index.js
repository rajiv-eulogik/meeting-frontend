import { ThemeProvider, createTheme } from "@mui/material/styles";

export const xTheme = createTheme({
    palette: {
        primary: {
            main: "#2a9461"
        },
        secondary: {
            main: "#494c7d"
        }
    }
});